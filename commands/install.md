---
description: Show install instructions for the ingitdb CLI
---

Invoke the `ingitdb:install` skill. It shows the user how to install the `ingitdb` CLI and then waits for them to run the install in their own shell — do not execute installers yourself.
