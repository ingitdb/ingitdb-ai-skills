# ingitdb describe view

Print the definition of a materialized view (source collections, columns, filters).

## Command

```bash
ingitdb describe view <view-name> [--format <yaml|json>] [--path <dir>]
```

## When to use

- Understand how a view is composed before relying on its output.

## Notes

- Read-only.
